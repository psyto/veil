{
  "name": "solana-privacy-suite",
  "version": "0.1.0",
  "private": true,
  "description": "Privacy-focused DeFi infrastructure for Solana",
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/*/sdk"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint",
    "clean": "yarn workspaces run clean"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
