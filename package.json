{
  "name": "veil",
  "version": "0.1.0",
  "private": true,
  "description": "Chain-agnostic privacy primitives for any blockchain, with optional Solana-specific extensions",
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/*/sdk",
    "apps/*/app",
    "apps/*/solver"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "clean": "yarn workspaces run clean"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
